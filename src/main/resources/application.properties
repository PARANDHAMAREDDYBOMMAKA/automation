server.port=${PORT:8080}
spring.application.name=worklog-automation
spring.thymeleaf.cache=false
logging.level.root=INFO
logging.level.com.kalvium=INFO

app.base.url=${APP_BASE_URL:http://localhost:8080}

# Database Configuration (Spring Boot DataSource)
# Set DATABASE_URL environment variable to your PostgreSQL/Supabase connection string
spring.datasource.url=${DATABASE_URL:postgresql://postgres.wmqoeaahrnboninrihkk:sunnyreddy2809@aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000

spring.task.scheduling.pool.size=2

server.tomcat.threads.max=20
server.tomcat.threads.min-spare=2
server.tomcat.max-connections=50

server.tomcat.connection-timeout=20000

management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never
